import XCTest
@testable import MarcsMessageWebSocketClient

final class MarcsMessageWebSocketClientTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MarcsMessageWebSocketClient().text, "Hello, World!")
    }
}
